[project]
name = "python-api"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fastapi>=0.111", "pydantic>=2.7", "sqlalchemy>=2.0", "alembic>=1.13"]

[tool.pytest.ini_options]
testpaths = ["tests"]
